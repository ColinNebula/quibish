# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# ============================================================
# DEPENDENCIES — never commit these, install with npm/yarn
# ============================================================
/node_modules
/.pnp
.pnp.js
backend/node_modules/

# ============================================================
# ENVIRONMENT VARIABLES — NEVER commit real .env files!
# .env.example files (no real secrets) are the only exceptions.
# ============================================================
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example

# ============================================================
# BUILD OUTPUT — generated files, not source
# ============================================================
/build
/dist
backend/dist/

# ============================================================
# RUNTIME / GENERATED DATA — not source code
# ============================================================
backend/data/*.json
uploads/
*.upload
*.tmp

# ============================================================
# COMPILED BINARIES (WASM, native addons)
# ============================================================
wasm/**/*.wasm
wasm/**/*.o
wasm/**/*.js.map
**/*.node

# ============================================================
# TESTING ARTEFACTS
# ============================================================
/coverage
*.lcov

# ============================================================
# LOGS
# ============================================================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
error.txt

# ============================================================
# OS / EDITOR GENERATED FILES
# ============================================================
.DS_Store
Thumbs.db
Desktop.ini
.vscode/
.idea/
*.swp
*.swo
*~

# ============================================================
# MISCELLANEOUS SENSITIVE / LARGE FILES
# ============================================================
# SSL certificates and private keys
*.pem
*.key
*.crt
*.p12
*.pfx

# Database files
*.sqlite
*.sqlite3
*.db

# Backup files
*.bak
*.backup

# Large media (user uploads should stay in uploads/ which is already ignored)
*.mp4
*.mov
*.avi
*.mkv
